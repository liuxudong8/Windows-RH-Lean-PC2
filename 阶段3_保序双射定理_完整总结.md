# 阶段3：保序双射定理形式化 — 完整总结

**日期**：2026-09-05
**文件**：`OrderPreservingBijection/stage_3.lean`（1445行）
**参考论文**：《保序双射定理6.pdf》（11页）
**Lean工具链**：v4.34.0-rc2（`C:\lt`）
**mathlib**：已编译（`C:\proj\mathlib4-master`）
**文档版本**：v2.0（含项目划分说明与第6节扩展）

---

## ⚠️ 项目划分说明（重要）

### 当前项目：第二篇论文 — 保序双射定理

**唯一目标**：证明保序双射定理（本原闭测地线 ↔ 本原素理想）

**包含内容**：
- 阶段1：双曲恒等式（工具）
- 阶段2：Q(√5)代数数论（基础）
- 阶段3：保序双射定理本身（核心目标）

**不包含**：
- ❌ Arthur 迹公式
- ❌ Jacquet-Langlands 对应
- ❌ Dolgopyat 混合
- ❌ Selberg zeta 函数的完整定义
- ❌ Riemann 猜想（RH）

### 下个项目：第三篇论文 — 谱对偶论证

**题目**：《基于 Arthur 稳定迹、Jacquet–Langlands 对应与测地流指数混合的谱对偶论证9》

**目标**：条件导出 RH

**已保留文件**：
- `OrderPreservingBijection/stage_4.lean`（RH谱对偶论证框架，**不删除**，下个项目继续使用）
- `阶段4_RH谱对偶论证_完整总结.md`（阶段4总结文档）

**引用关系**：第三篇论文将引用第二篇论文的保序双射定理作为前置基础。

---

## 一、阶段3概述

### 1.1 目标

在 Lean 4 + mathlib4 中形式化"保序双射定理"（第二篇论文），建立算术双曲3流形 `PSL₂(O_K)` 本原闭测地线与 `Q(√5)` 素理想之间的保序双射，证明长度恒等式 `ℓ(γ) = log Nm(Φ(γ))`。

### 1.2 核心主定理（论文第1.2节）

存在映射 `Φ: 𝒢 → I_prim`，满足：
1. **双向双射**（单射 + 满射）
2. **保序**：`ℓ(γ₁) < ℓ(γ₂) ⇒ Nm(Φ(γ₁)) < Nm(Φ(γ₂))`
3. **长度-范数恒等式**：`ℓ(γ) = log Nm(Φ(γ))`

### 1.3 代数核心策略

采用"代数核心优先"策略：
- **本原闭测地线**定义为**本原非单位代数元**的子类型
- 几何事实（PSL₂共轭类、双曲长度）作为接口后续引入
- 复用阶段2的Q(√5)代数数论成果

---

## 二、核心数学思想

### 2.1 素元 vs 本原元（关键澄清）

论文版本6的重要更正：
- **素元（prime element）**：环中的元素概念，素元生成的主理想是素理想
- **本原元（primitive element）**：不能写成 `β^m`（`m≥2`），本原≠素元
- **素元 ↔ 素理想**（在PID中，不计单位乘子）
- **本原元 ↔ 本原闭测地线**，但本原元并不直接对应素理想

**反例**：`π₁·π₂`（两个不同素元相乘）是本原元，但生成的理想是乘积理想，不是素理想。

### 2.2 两种等价关系（论文版本6勘误）

1. **单位等价 `~_u`**：`α ~_u β` 当且仅当 `∃ u ∈ O_K^×, α = u·β`
2. **双曲等价 `~_h`**：`α ~_h β` 当且仅当 `(tr α)² = (tr β)²` 且 `Nm(α) = Nm(β)`

**重要勘误**：单位等价**不蕴含**双曲等价！

**显式反例**：取 `u=φ`, `β=1`, `α=φ·1=φ`
- `α ~_u β`（因为 `α = φ * β`，φ是单位）
- 但 `tr(α)=1`, `tr(β)=2`, `(tr α)²=1 ≠ 4=(tr β)²`
- 所以 `α ≁_h β`

**原因**：`g_{εα} = diag(εα, εᾱ)` 是更换矩阵对角元得到的新矩阵，**不是**对 `g_α` 做矩阵共轭的结果。`ε ∈ O_K^×` 是环中的代数元素，**不是** GL₂ 矩阵。

### 2.3 PSL₂共轭类判定

`PSL₂(R) = SL₂(R)/{±I}`，其中 `g` 与 `-g` 等同：
- `tr(-g) = -tr(g)`，`det(-g) = det(g)`
- 因此 PSL₂ 的双曲共轭类由**迹的平方**+行列式决定，而不是直接由迹决定

---

## 三、定义汇总

### 3.1 基础定义（第3.1节）

| 定义 | 类型 | 数学含义 |
|------|------|----------|
| `IsPrimitive α` | `Prop` | α不能表示为`β^m`（`m≥2`） |
| `IsNonUnit α` | `Prop` | `Int.natAbs (norm α) > 1` |
| `PrimitiveElements` | `Set GoldenInt` | 本原非单位元集合 |
| `PrimitiveGeodesic` | `Type` | 本原非单位元的子类型 |
| `PrimitiveGeodesic.element γ` | `GoldenInt` | 底层代数元 |

### 3.2 等价关系（第3.2节）

| 定义 | 类型 | 数学含义 |
|------|------|----------|
| `elementTrace α` | `GoldenInt` | `α + star α`（迹） |
| `UnitEquiv α β` | `Prop` | `∃ u, IsUnit u ∧ α = u * β` |
| `HyperbolicEquiv α β` | `Prop` | `(trace α)² = (trace β)² ∧ norm α = norm β` |

### 3.3 范数与长度（第4节）

| 定义 | 类型 | 数学含义 |
|------|------|----------|
| `elementNorm α` | `ℤ` | `GoldenInt.norm α`（元素范数） |
| `principalIdealNorm α` | `ℕ` | `Int.natAbs (norm α)`（主理想范数） |
| `geodesicLength α h` | `ℝ` | `Real.log (norm α : ℝ)`（需`norm α > 0`） |
| `geodesicLengthPrime γ` | `ℝ` | `Real.log (principalIdealNorm γ.element : ℝ)`（素元测地线长度） |

### 3.4 素元与素理想（第5节）

| 定义 | 类型 | 数学含义 |
|------|------|----------|
| `IsPrimeElement α` | `Prop` | `Prime α`（素元） |
| `PrimeGeodesic` | `Type` | `{α // IsPrimeElement α ∧ IsNonUnit α}`（素元测地线） |
| `IsPrimeIdeal P` | `Prop` | `P.IsPrime ∧ P ≠ ⊥ ∧ P ≠ ⊤` |
| `PrimeIdeals` | `Set (Ideal GoldenInt)` | 非零非单位素理想集合 |
| `Phi γ` | `Ideal GoldenInt` | `Ideal.span {γ.element}`（双射映射） |

### 3.5 第6节扩展：χ₅特征与数域定义

| 定义 | 类型 | 数学含义 |
|------|------|----------|
| `chi5 n` | `ℤ` | Kronecker特征χ₅(n)（模5二次特征） |
| `chi5UnitFun u` | `Units ℂ` | χ₅在ZMod 5单位群上的函数 |
| `chi5UnitHom` | `Units (ZMod 5) →* Units ℂ` | χ₅的单位群同态 |
| `chi5Dirichlet` | `DirichletCharacter ℂ 5` | χ₅作为mathlib的Dirichlet特征 |
| `x2_minus_5` | `Polynomial ℚ` | 多项式X²-5 |
| `Qsqrt5` | `Type` | `AdjoinRoot x2_minus_5`（Q(√5)作为mathlib数域） |
| `dedekindCoeff n` | `ℂ` | Dedekind zeta的系数函数 |
| `numIdealsNormLe s` | `ℕ` | 范数≤s的整理想个数 |
| `riemannZetaReal s` | `ℂ` | 直接复用mathlib的riemannZeta |
| `dirichletLReal s` | `ℂ` | 直接复用mathlib的L函数 |
| `dedekindZetaReal s` | `ℂ` | 直接复用mathlib的NumberField.dedekindZeta |

---

## 四、定理汇总

### 4.1 阶段2成果复用

| 定理 | 内容 | 来源 |
|------|------|------|
| `goldenInt_is_PID` | `IsPrincipalIdealRing GoldenInt` | 阶段2 |
| `prime_ideal_principal` | 非零素理想是主理想 | 阶段2 |

### 4.2 等价关系性质

| 定理 | 内容 |
|------|------|
| `unitEquiv_refl` | 单位等价自反 |
| `unitEquiv_symm` | 单位等价对称 |
| `unitEquiv_trans` | 单位等价传递 |
| `hyperbolicEquiv_refl` | 双曲等价自反 |
| `hyperbolicEquiv_symm` | 双曲等价对称 |
| `hyperbolicEquiv_trans` | 双曲等价传递 |
| `unitEquiv_not_implies_hyperbolicEquiv` | **单位等价不蕴含双曲等价**（显式反例） |
| `unitEquiv_preserves_norm_abs` | 单位等价保持范数绝对值 |

### 4.3 迹计算

| 定理 | 内容 |
|------|------|
| `trace_phi` | `tr(φ) = 1` |
| `trace_one` | `tr(1) = 2` |

### 4.4 本原元性质

| 定理 | 内容 |
|------|------|
| `nonprimitive_if_power` | 若`α=β^m`（`m≥2`），则α不是本原元 |
| `primitive_element_iff_primitive_geodesic` | 本原非单位元 ↔ 本原测地线 |

### 4.5 长度-范数恒等式

| 定理 | 内容 |
|------|------|
| `length_norm_identity` | `ℓ(γ_α) = log Nm(α)`（需`norm α > 0`） |
| `prime_ideal_norm_eq_abs_element_norm` | 素理想范数 = 元素范数绝对值 |
| `length_norm_identity_prime` | `ℓ(γ) = log Nm(Φ(γ))`（素元版本） |
| `prime_ideal_norm_gt_one` | 非单位素元的素理想范数 > 1 |

### 4.6 素元性质

| 定理 | 内容 | 证明方法 |
|------|------|----------|
| `prime_element_is_primitive` | 素元是本原元 | 不可约性：素元→不可约，`α=β^m`→可约，矛盾 |
| `prime_element_generates_prime_ideal` | 素元生成素理想 | mathlib现成定理 |

### 4.7 双射性质（主定理核心）✅ 完全证明

| 定理 | 内容 | 证明方法 |
|------|------|----------|
| `Phi_maps_to_prime_ideals` | Φ映射到素理想 | 素元生成素理想 + 素元非零非单位 |
| `Phi_injective` | Φ单射（模单位等价） | 主理想相等→互相整除→差一个单位 |
| `Phi_surjective` | Φ满射 | PID性质→素理想是主理想→生成元是素元 |

### 4.8 保序性 ✅ 完全证明

| 定理 | 内容 | 证明方法 |
|------|------|----------|
| `order_preserving` | `ℓ(γ₁) < ℓ(γ₂) ⇒ Nm(Φ(γ₁)) < Nm(Φ(γ₂))` | 长度-范数恒等式 + 对数严格单调递增 |

### 4.9 主定理 ✅ 完全证明

| 定理 | 内容 |
|------|------|
| `main_theorem_bijection` | 双射（良定义 + 单射 + 满射） |
| `main_theorem_full` | **完整主定理**（双射 + 保序 + 长度-范数恒等式） |

### 4.10 第6节：χ₅特征与Euler因子

| 定义/定理 | 内容 | 证明方法 | 状态 |
|-----------|------|----------|------|
| `chi5_multiplicative` | χ₅完全乘性 | 25种情况逐一验证 | ✅ |
| `chi5_ramified` | χ₅(5) = 0 | 直接计算 | ✅ |
| `chi5_split` | p≡±1 mod 5 ⇒ χ₅(p) = 1 | 按p mod 5分类 | ✅ |
| `chi5_inert` | p≡±2 mod 5 ⇒ χ₅(p) = -1 | 按p mod 5分类 | ✅ |
| `chi5UnitFun_val` | χ₅单位函数值 | 有限枚举 | ✅ |
| `chi5UnitFun_mul` | χ₅单位函数乘性 | 有限枚举 | ✅ |
| `complex_exp_log_power` | `exp(-s·log N) = N^{-s}` | `Complex.log_inv`定理 | ✅ |
| `inert_power_identity` | `(p^{-2})^s = (p^{-s})^2` | `calc`+`complex_exp_log_power` | ✅ |
| `euler_factors_match` | ζ_K因子 = ζ·L因子（逐素数） | 5种情况分析 | ✅ |
| `x2_minus_5_irreducible` | X²-5不可约 | Eisenstein判别法(p=5) | ✅ |
| `riemann_zeta_converges` | Riemann zeta收敛 | mathlib定理 | ✅ |
| `dirichlet_L_converges` | Dirichlet L收敛 | mathlib定理 | ✅ |
| `numIdealsNormLe_monotone` | 理想计数单调 | 集合包含 | ✅ |
| `dedekindCoeff_growth` | 系数增长估计 | ⚠️ sorry |
| `numIdealsNormLe_growth` | 理想计数增长估计 | ⚠️ sorry |
| `dedekind_zeta_converges` | Dedekind zeta收敛 | ⚠️ sorry |
| `dedekind_zeta_factorization_real` | ζ_K=ζ·L（真实版本） | ⚠️ sorry |
| `selberg_zeta_eq_dedekind_zeta_real` | Selberg=Dedekind（真实版本） | ⚠️ sorry |

---

## 五、关键证明思路

### 5.1 素元是本原元（`prime_element_is_primitive`）

**数学思路**：
1. 素元 → 不可约（mathlib定理：`Prime α → Irreducible α`）
2. 假设 `α = β^m`（`m ≥ 2`）
3. 则 `α = β · β^(m-1)`（幂次分解）
4. `¬IsUnit β`：否则 `α = β^m` 是单位，与素元矛盾
5. `¬IsUnit(β^(m-1))`：否则 `β` 是单位（因为 `β | β^(m-1)`，单位的因子是单位）
6. 所以 `α` 可写成两个非单位之积 → `¬Irreducible α`
7. 矛盾！

**Lean技巧**：
- 使用 `Irreducible` 而非直接处理幂次，避免了 `IsUnit` 的析构问题
- 利用"单位的因子是单位"（`exact?`可证）

### 5.2 Φ单射（`Phi_injective`）

**数学思路**：
1. `Φ(γ₁) = Φ(γ₂)` ⇒ `(α₁) = (α₂)`
2. 主理想相等 ⇒ `α₁ | α₂` 且 `α₂ | α₁`
3. 所以 `α₂ = α₁ · u`，`α₁ = α₂ · v`
4. 代入得 `α₁ = α₁ · (u · v)`
5. 因为 `α₁ ≠ 0`（素元非零），消去得 `1 = u · v`
6. 所以 `u` 是单位
7. 因此 `α₂ = u · α₁`，即 `UnitEquiv α₂ α₁`
8. 由对称性得 `UnitEquiv α₁ α₂`

### 5.3 Φ满射（`Phi_surjective`）

**数学思路**：
1. 任取 `P ∈ PrimeIdeals`，则 `P.IsPrime` 且 `P ≠ ⊥`
2. 由PID性质（阶段2），存在 `α` 使得 `P = (α)`
3. 证明 `α` 是素元：
   - `α ≠ 0`（因为 `P ≠ ⊥`）
   - `¬IsUnit α`（因为 `P ≠ ⊤`）
   - `α | a·b ⇒ α | a ∨ α | b`（因为 `P.IsPrime`）
4. 证明 `IsNonUnit α`：
   - `norm α ≠ 0`（因为 `α ≠ 0`）
   - `norm α ≠ ±1`（因为 `¬IsUnit α`，阶段2的`isUnit_iff_norm`）
   - 所以 `Int.natAbs(norm α) > 1`
5. 构造 `γ = ⟨α, h_prime, h_nonunit⟩`
6. 则 `Φ(γ) = (α) = P`

### 5.4 保序性（`order_preserving`）

**数学思路**：
1. `ℓ(γ₁) < ℓ(γ₂)`
2. 由长度-范数恒等式：`ℓ(γ) = log Nm(Φ(γ))`
3. 所以 `log Nm(Φ(γ₁)) < log Nm(Φ(γ₂))`
4. 因为 `Nm(Φ(γᵢ)) > 0`（素理想范数是正自然数）
5. 对数函数在 `x > 0` 上严格单调递增
6. 所以 `Nm(Φ(γ₁)) < Nm(Φ(γ₂))`

**Lean技巧**：
- 使用 `principalIdealNorm`（绝对值）而非 `norm`，因为素元范数可能为负
- 使用 `Real.log_lt_log_iff` 定理

### 5.5 第6节：Euler因子匹配（`euler_factors_match`）

**数学思路**：
对每个有理素数p，证明ζ_K的Euler因子等于ζ·L的Euler因子：

1. **p=5（分歧）**：χ₅(5)=0，L因子=1，乘积=ζ因子=`(1-5^{-s})^{-1}`
2. **p≡±1（分裂）**：χ₅(p)=1，L因子=`(1-p^{-s})^{-1}`，乘积=`(1-p^{-s})^{-2}`
3. **p≡±2（惯性）**：χ₅(p)=-1，L因子=`(1+p^{-s})^{-1}`，乘积=`(1-p^{-2s})^{-1}`

**关键引理**：
- `complex_exp_log_power`：`exp(-s·log N) = N^{-s}`，使用`Complex.log_inv`定理（正实数arg=0≠π）
- `inert_power_identity`：`(p^{-2})^s = (p^{-s})^2`，使用`calc`+`complex_exp_log_power`+`Complex.exp_add`

---

## 六、与论文的对应关系

| 论文章节 | 内容 | Lean形式化 |
|----------|------|------------|
| 第2节 | Q(√5)代数数论基础 | 阶段2成果复用 |
| 第2.4节 | 本原元定义 | `IsPrimitive`, `PrimitiveElements` |
| 第3.1节 | SL₂共轭分类基础 | `elementTrace`, `HyperbolicEquiv` |
| 第3.2节 | 一一对应引理（两种等价关系） | `UnitEquiv`, `HyperbolicEquiv`, 反例 |
| 第3.3节 | 本原元与本原轨道等价 | `primitive_element_iff_primitive_geodesic` |
| 第4节 | H₃轴测地线长度恒等式 | `geodesicLength`, `length_norm_identity` |
| 第5.1节 | 单射证明 | `Phi_injective` |
| 第5.2节 | 满射证明 | `Phi_surjective` |
| 第5.3节 | 保序证明 | `order_preserving` |
| 第5.4节 | 恒等式条件 | `length_norm_identity_prime` |
| 第6.1节 | Selberg zeta与Dedekind zeta等价 | `selberg_zeta_eq_dedekind_zeta`（代数核心） |
| 第6.2节 | ζ_K = ζ·L(χ₅)分解 | `dedekind_zeta_factorization`（代数核心） |

---

## 七、编译与验证

### 7.1 编译命令

```powershell
cd C:\proj
$env:PATH = "C:\lt\bin;$env:PATH"
$env:LEAN_TOOLCHAIN = "C:\lt"
$env:LAKE_BUILD_JOBS=1
lake build OrderPreservingBijection.stage_3
```

### 7.2 编译结果

- **核心部分（第1-5节）**：✅ 完全证明，无 sorry
- **第6节扩展（分析部分）**：5个 sorry（收敛性、增长估计、真实zeta函数版本）
- **文件总行数**：1445行
- **无编译错误**

### 7.3 源文件位置

- 源文件：`C:\Users\shtcl\Doubao\chats\2026-08-27\new-chat\OrderPreservingBijection\stage_3.lean`
- 编译副本：`C:\proj\OrderPreservingBijection\stage_3.lean`

---

## 八、已知限制与下一步

### 8.1 当前限制

1. **代数核心版本**：本原闭测地线定义为本原非单位代数元的子类型，尚未引入完整的PSL₂共轭类几何结构
2. **强逼近定理**：从PSL₂(K)到PSL₂(O_K)的过渡需要强逼近定理，mathlib中可能没有形式化，当前作为接口预留
3. **分裂素等长轨道**：`p ≡ ±1 (mod 5)` 分裂为两个素理想，对应两条等长轨道，保序性中`ℓ(γ₁) = ℓ(γ₂)`不触发严格保序条件
4. **范数为负的素元**：使用`principalIdealNorm`（绝对值）处理，几何上对应取`α²`（二重缠绕）
5. **第6节分析部分**：收敛性、增长估计、真实zeta函数版本仍有5个sorry，属于分析细节，不影响保序双射定理核心

### 8.2 下一步计划（当前项目内）

1. **第6节分析细节完善**（可选）
   - 完善`dedekindCoeff_growth`、`numIdealsNormLe_growth`等增长估计
   - 完善`dedekind_zeta_converges`收敛性证明
   - 完善`dedekind_zeta_factorization_real`真实版本

2. **几何接口完善**（可选）
   - 引入PSL₂共轭类的完整定义
   - 建立代数元与共轭类的精确对应
   - 形式化强逼近定理（或作为公理）

### 8.3 下个项目（第三篇论文）

**题目**：《基于 Arthur 稳定迹、Jacquet–Langlands 对应与测地流指数混合的谱对偶论证9》

**已保留文件**：
- `OrderPreservingBijection/stage_4.lean`（RH谱对偶论证框架）
- `阶段4_RH谱对偶论证_完整总结.md`

**核心前提（9个）**：
1. 保序双射定理（当前项目成果，作为前置基础）
2. Arthur 稳定迹公式
3. 轨道权重化简
4. 分裂素 JL 局部权重补偿
5. JL 酉提升
6. 自伴算子谱定理
7. Dolgopyat 混合（兜底）
8. 磨光测试函数分析
9. 连续谱平凡零点

---

## 九、关键数学事实速查

### 9.1 Q(√5)基本事实

- 整数环：`O_K = ℤ[φ]`，`φ = (1+√5)/2`
- 范数：`Nm(a+bφ) = a² + ab - b²`
- 基本单位：`φ`，`Nm(φ) = -1`
- 类数：`h_K = 1`（PID）
- 素理想三分歧：
  - 分歧素：`p = 5`，`(5) = 𝔭₅²`，`Nm(𝔭₅) = 5`
  - 分裂素：`p ≡ ±1 (mod 5)`，`(p) = 𝔭·𝔭̄`，`Nm(𝔭) = p`
  - 惯性素：`p ≡ ±2 (mod 5)`，`(p) = 𝔭`，`Nm(𝔭) = p²`

### 9.2 双曲几何基本事实

- 双曲3上半空间：`ℍ³ = {(x,y,z) | z > 0}`，度量 `ds² = (dx²+dy²+dz²)/z²`
- 对角双曲元 `g_α = diag(α, ᾱ)` 的轴测地线长度：
  `ℓ(g) = 2 cosh⁻¹((√t + 1/√t)/2)`，其中 `t = det(g) = Nm(α)`
- 双曲恒等式（阶段1）：`2 cosh⁻¹((√t + 1/√t)/2) = log t`（`t > 0`）
- 因此：`ℓ(γ_α) = log Nm(α)`

### 9.3 χ₅特征基本事实

- 定义：`χ₅(n) = 0`（若5|n），`1`（若n≡±1 mod 5），`-1`（若n≡±2 mod 5）
- 完全乘性：`χ₅(mn) = χ₅(m)·χ₅(n)`
- 偶特征：`χ₅(-1) = χ₅(4) = 1`
- 非平凡：`χ₅(2) = -1 ≠ 1`
- 与素理想三分歧的对应：
  - 分歧素p=5：χ₅(p)=0
  - 分裂素p≡±1：χ₅(p)=1
  - 惯性素p≡±2：χ₅(p)=-1

---

**文档结束**

*阶段3核心目标已完成：保序双射定理（双射+保序+长度-范数恒等式）已完全形式化并验证通过，核心部分（第1-5节）无sorry。第6节扩展内容（χ₅特征、Euler因子匹配、数域定义、收敛性分析）的代数核心已建立，剩余5个分析细节sorry不影响主定理。stage_4文件已保留，供下个项目（第三篇论文）继续使用。*
